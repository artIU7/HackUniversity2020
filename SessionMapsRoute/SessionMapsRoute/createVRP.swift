//
//  createVRP.swift
//  SessionMapsRouting
//
//

import Foundation
import NMAKit

extension ViewController {
/*func vrpJson() {
    var pointHeli = NMAGeoCoordinates()
    let pJSON = jsonFromfile("vrp.json")
    var dictonary : NSDictionary?
    if let data = pJSON.data(using: String.Encoding.utf8) {
        do {
            dictonary = JSONSerialization.jsonObject(with: data, options: JSONSerialization.ReadingOptions)
            if let myDictionary = dictonary
            {
                print(" First name is: \(myDictionary["first_name"]!)")
            }
        } catch let error as NSError {
            print(error)
        }
    }
}

*/
}
